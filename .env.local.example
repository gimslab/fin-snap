# Fin-Snap 환경 변수 예시
# 이 파일을 복사하여 .env.local 로 사용하세요: cp .env.local.example .env.local
# .env.local 은 .gitignore 에 포함되어 있어 Git에 커밋되지 않습니다.

# -------------------------------------------------------
# ⚠️  API Key 보안 안내
# -------------------------------------------------------
# 이 프로젝트는 API Key를 사용자 브라우저(LocalStorage)에 저장합니다.
# 아래 서버 측 환경 변수는 선택 사항이며,
# 서버 프록시 방식으로 전환할 경우에만 사용합니다.
# -------------------------------------------------------

# Google Gemini API Key (선택)
# GEMINI_API_KEY=your_gemini_api_key_here

# OpenAI API Key (선택)
# OPENAI_API_KEY=your_openai_api_key_here

# 앱 버전 (빌드 정보 표시용)
NEXT_PUBLIC_APP_VERSION=0.1.0
